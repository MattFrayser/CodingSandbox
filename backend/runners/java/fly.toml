app = 'codr-java-runner'
primary_region = 'iad'

[build]
  dockerfile = 'Dockerfile'

[env]
  LANGUAGE = "java"

# Keep worker running
min_machines_running = 1
auto_start_machines = true

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
