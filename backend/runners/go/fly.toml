app = 'codr-go-runner'
primary_region = 'iad'

[build]
  dockerfile = 'Dockerfile'

[env]
  LANGUAGE = "go"

# Keep worker running
min_machines_running = 1
auto_start_machines = true

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1